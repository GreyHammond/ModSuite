discord.py>=2.4.0
python-dotenv>=1.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
aiohttp>=3.9.0
psutil>=5.9.0

# auth.py calls the Discord OAuth2 endpoints with httpx. It was never declared,
# so a fresh venv crashed on startup with ModuleNotFoundError: httpx. Existing
# installs only worked because something else had pulled it in transitively.
httpx>=0.27.0

# api.py builds its request models on pydantic directly. FastAPI depends on it
# too, but declaring it means a FastAPI major bump cannot silently change the
# version underneath the models.
pydantic>=2.6.0

# auth.py imports starlette.middleware.base directly, for the same reason.
starlette>=0.36.0
